{
  "DocumentName": "AWS-RunShellScript",
  "InstanceIds": ["i-0be6845d8493d077a"],
  "Parameters": {
    "commands": [
      "set -euxo pipefail",
      "date",
      "free -h",
      "swapon --show || true",
      "cd /opt/erpnext/frappe_docker",
      "docker-compose -f pwd.yml -f compose.public.yml ps",
      "docker ps -a --format 'table {{.Names}}\t{{.Status}}\t{{.Ports}}'",
      "docker logs --tail 120 frappe_docker-frontend-1 || true",
      "docker logs --tail 120 frappe_docker-websocket-1 || true",
      "docker logs --tail 120 frappe_docker-backend-1 || true",
      "ss -lntp | grep -E ':80 |:443 |:8000 ' || true"
    ]
  }
}
